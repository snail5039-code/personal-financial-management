# 나중에 할 일

## 직접 테스트해봐야 할 것 (Claude가 대신 켤 수 없는 것들)

- **트레이 앱 직접 켜보기** — `python tray_app.py` 실행해서 트레이 아이콘 확인, "커리마 열기"/"종료" 메뉴 확인.
  Claude가 코드로는 다 검증했지만, 실제로 며칠 켜두고 자연스러운지는 직접 써봐야 앎.
- **Gmail·캘린더 첫 OAuth 로그인** — 아래 항목대로 본인 계정으로 직접 로그인해야 함 (Claude가 대신 못 함).
- **마이크로 "커리마"라고 실제로 불러보기** — Claude는 마이크가 없어서 합성 음성으로만 테스트함(실제
  사람 목소리로는 정확도가 더 좋을 가능성이 높음). "삐" 소리 후 명령까지 말해보고 잘 알아듣는지,
  트레이 메뉴에서 음성 인식 켜고/끄고가 되는지 확인 필요. 첫 실행 시 Whisper 모델을 내려받느라
  몇 초~몇십 초 걸릴 수 있음(인터넷 필요, 이후엔 오프라인).
- **"내 자료 색인해줘"로 실제 색인 돌려보기** — Claude는 실제 사용자 Documents/Desktop 폴더를
  색인하면 시간이 오래 걸리고 API 비용도 들어서, 가짜 테스트 문서로만 검증하고 진짜 폴더는
  안 건드림. 파일이 몇 개나 있는지에 따라 첫 색인이 좀 걸릴 수 있음.
- **일정 알림(선제적 제안) 실제 캘린더로 확인** — Gmail·캘린더 OAuth 로그인부터 하고, 구글
  캘린더에 곧 시작할 일정을 하나 넣어둔 뒤 트레이 앱을 켜서 실제로 알림이 오는지 확인.
  Claude는 가짜 캘린더 응답으로만 로직을 검증함.

## Gmail · 구글 캘린더 연동 마무리 (코드는 이미 완성, 설정만 남음)

`tools_gmail.py` / `tools_calendar.py` / `google_gmail.py` / `google_calendar.py`는 이미 구현·테스트
완료. 아래 두 가지만 하면 바로 쓸 수 있다.

1. [Google Cloud Console](https://console.cloud.google.com) → 기존 프로젝트(할일 연동에 쓰던 것과 동일)
   → **API 및 서비스 → 라이브러리**에서 `Gmail API`, `Google Calendar API` 사용 설정
   - 새 `credentials.json`은 필요 없음 (기존 것 재사용)
2. 커리마에서 "오늘 메일 확인해줘" / "오늘 일정 뭐 있어?"라고 말하면 각각 브라우저가 한 번씩 열림
   → 본인 구글 계정으로 로그인 + 권한 허용
   - "확인되지 않은 앱" 경고가 뜨면 **고급 → (앱 이름)으로 이동(안전하지 않음)** 클릭 (본인이 만든 개인용 앱이라 정상)

자세한 설명은 [README.md](README.md)의 "Gmail · 구글 캘린더 연동 (선택)" 절 참고.

## 트레이 앱을 컴퓨터 켤 때 자동으로 띄우고 싶다면

`tray_app.py`는 만들어져 있지만 자동 시작 등록은 안 돼 있음 (직접 하는 설정이라 스크립트가 안 건드림).

1. `tray_app.py`의 바로가기(shortcut) 만들기
2. `Win+R` → `shell:startup` 입력 → 열린 폴더에 바로가기 붙여넣기

## 로드맵상 다음 단계들

자세한 내용/근거는 대화 중 만든 [커리마 진화 로드맵](https://claude.ai/code/artifact/ca3b191b-bd63-4eb1-bd08-2f5a6a336e08) 아티팩트 참고.

- ~~**Phase 4-1** — 상시 실행 여부 결정 → 자동화 · 알림~~ **완료** (`tray_app.py`: 아침 브리핑 + 다운로드 알림)
- ~~**Phase 4-2** — 음성 웨이크워드~~ **완료** ("커리마"라고 부르면 응답, `voice_assistant.py`).
  위험한 동작(앱 강제종료·파일 삭제 등)은 음성에서 실행 안 하고 대화형 앱으로 안내함.
- ~~**Phase 4-3** — 선제적 제안~~ **완료** (`tray_app.py`의 `_proactive_loop`: 캘린더 일정이
  설정된 시간 전이 되면 먼저 알림, 중복 알림 방지, 캘린더 미연동 시 조용히 건너뜀).
- ~~**Phase 5** — Local Search · 개인 지식베이스(RAG)~~ **완료** (`local_index.py`/`tools_local.py`:
  문서·바탕화면 폴더 색인 + 의미 기반 검색). ~~Computer Use~~는 범위에서 제외하기로 함
  (비밀번호/결제 화면 자동 클릭 위험 때문에 하지 않기로 결정).

**로드맵 전체 완료.** 남은 건 전부 위의 "직접 테스트해봐야 할 것" 항목들뿐이다.
